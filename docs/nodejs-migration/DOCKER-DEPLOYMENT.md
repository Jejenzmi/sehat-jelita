# SIMRS ZEN - Docker Deployment Guide

## 📋 Daftar Isi

1. [Persyaratan Sistem](#persyaratan-sistem)
2. [Persiapan Environment](#persiapan-environment)
3. [Build & Deploy](#build--deploy)
4. [Konfigurasi Production](#konfigurasi-production)
5. [Monitoring & Maintenance](#monitoring--maintenance)
6. [Troubleshooting](#troubleshooting)

---

## 🖥️ Persyaratan Sistem

### Minimum Requirements
| Resource | Minimum | Recommended |
|----------|---------|-------------|
| CPU | 2 vCPU | 4 vCPU |
| RAM | 4 GB | 8 GB |
| Storage | 40 GB SSD | 100 GB SSD |
| OS | Ubuntu 22.04 LTS | Ubuntu 22.04 LTS |

### Software Requirements
- Docker Engine 24.0+
- Docker Compose 2.20+
- Git

---

## 🔧 Persiapan Environment

### 1. Install Docker (Ubuntu)

```bash
# Update system
sudo apt update && sudo apt upgrade -y

# Install prerequisites
sudo apt install -y apt-transport-https ca-certificates curl software-properties-common

# Add Docker GPG key
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg

# Add Docker repository
echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# Install Docker
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin

# Add user to docker group
sudo usermod -aG docker $USER
newgrp docker

# Verify installation
docker --version
docker compose version
```

### 2. Clone Repository

```bash
cd /opt
sudo mkdir -p simrs-zen && sudo chown $USER:$USER simrs-zen
cd simrs-zen

# Clone repository
git clone https://github.com/your-org/simrs-zen-backend.git .
```

### 3. Setup Environment Variables

```bash
# Copy example environment file
cp .env.example .env

# Edit environment variables
nano .env
```

**Isi file `.env`:**

```env
# ===========================================
# SIMRS ZEN - Production Environment
# ===========================================

# Server Configuration
NODE_ENV=production
PORT=3000

# Database Configuration
DB_USER=simrs_admin
DB_PASSWORD=your_secure_password_here
DB_NAME=simrs_zen_production

# Full database URL (auto-generated by docker-compose)
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@postgres:5432/${DB_NAME}

# Redis Configuration
REDIS_URL=redis://redis:6379

# JWT Configuration (minimum 32 characters)
JWT_SECRET=your_super_secret_jwt_key_minimum_32_characters_long
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# Frontend URL (for CORS)
FRONTEND_URL=https://simrszen.id

# ===========================================
# BPJS Integration (Wajib untuk RS BPJS)
# ===========================================
BPJS_CONS_ID=your_cons_id
BPJS_SECRET_KEY=your_secret_key
BPJS_USER_KEY=your_user_key
BPJS_BASE_URL=https://apijkn.bpjs-kesehatan.go.id

# BPJS Antrean
BPJS_ANTREAN_CONS_ID=your_antrean_cons_id
BPJS_ANTREAN_SECRET_KEY=your_antrean_secret_key
BPJS_ANTREAN_USER_KEY=your_antrean_user_key

# ===========================================
# SATU SEHAT Integration
# ===========================================
SATU_SEHAT_CLIENT_ID=your_client_id
SATU_SEHAT_CLIENT_SECRET=your_client_secret
SATU_SEHAT_ORG_ID=your_organization_id
SATU_SEHAT_ENV=production  # sandbox | production

# ===========================================
# Email Configuration (SMTP)
# ===========================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=noreply@simrszen.id
SMTP_PASS=your_app_password
SMTP_FROM="SIMRS ZEN <noreply@simrszen.id>"

# ===========================================
# Storage Configuration
# ===========================================
STORAGE_TYPE=local  # local | s3
STORAGE_PATH=/app/storage

# S3 Configuration (if STORAGE_TYPE=s3)
S3_ENDPOINT=
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_BUCKET=simrs-zen-files
S3_REGION=ap-southeast-1
```

---

## 🚀 Build & Deploy

### Production Deployment (Full Stack)

```bash
# Build dan start semua services
docker compose --profile production up -d --build

# Lihat status containers
docker compose ps

# Lihat logs
docker compose logs -f
```

### Development/Staging Deployment (tanpa Nginx)

```bash
# Development mode
docker compose up -d --build

# Lihat logs realtime
docker compose logs -f api
```

### Services yang Dijalankan

| Service | Port | Deskripsi |
|---------|------|-----------|
| `postgres` | 5432 | PostgreSQL Database |
| `redis` | 6379 | Redis Cache |
| `api` | 3000 | Node.js API Server |
| `nginx` | 80, 443 | Reverse Proxy (production) |

---

## ⚙️ Konfigurasi Production

### 1. Setup SSL dengan Certbot

```bash
# Install Certbot
sudo apt install -y certbot

# Generate SSL certificate
sudo certbot certonly --standalone -d api.simrszen.id

# Copy certificates ke folder ssl
sudo mkdir -p /opt/simrs-zen/ssl
sudo cp /etc/letsencrypt/live/api.simrszen.id/fullchain.pem /opt/simrs-zen/ssl/
sudo cp /etc/letsencrypt/live/api.simrszen.id/privkey.pem /opt/simrs-zen/ssl/
sudo chown $USER:$USER /opt/simrs-zen/ssl/*
```

### 2. Nginx Configuration

Buat file `nginx.conf`:

```nginx
worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
    use epoll;
    multi_accept on;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # Logging
    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';
    access_log /var/log/nginx/access.log main;

    # Performance
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;

    # Gzip Compression
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml application/json application/javascript 
               application/rss+xml application/atom+xml image/svg+xml;

    # Rate Limiting
    limit_req_zone $binary_remote_addr zone=api_limit:10m rate=30r/s;
    limit_conn_zone $binary_remote_addr zone=conn_limit:10m;

    # Upstream API
    upstream api_servers {
        server api:3000;
        keepalive 32;
    }

    # HTTP -> HTTPS Redirect
    server {
        listen 80;
        server_name api.simrszen.id;
        
        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }
        
        location / {
            return 301 https://$host$request_uri;
        }
    }

    # HTTPS Server
    server {
        listen 443 ssl http2;
        server_name api.simrszen.id;

        # SSL Configuration
        ssl_certificate /etc/nginx/ssl/fullchain.pem;
        ssl_certificate_key /etc/nginx/ssl/privkey.pem;
        ssl_session_timeout 1d;
        ssl_session_cache shared:SSL:50m;
        ssl_session_tickets off;

        # Modern SSL Configuration
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256;
        ssl_prefer_server_ciphers off;

        # HSTS
        add_header Strict-Transport-Security "max-age=63072000" always;

        # Security Headers
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;

        # API Proxy
        location /api {
            limit_req zone=api_limit burst=50 nodelay;
            limit_conn conn_limit 30;

            proxy_pass http://api_servers;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
            
            # Timeouts
            proxy_connect_timeout 60s;
            proxy_send_timeout 60s;
            proxy_read_timeout 60s;

            # Buffer settings
            proxy_buffer_size 128k;
            proxy_buffers 4 256k;
            proxy_busy_buffers_size 256k;
        }

        # WebSocket for Socket.IO
        location /socket.io {
            proxy_pass http://api_servers;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_read_timeout 86400;
        }

        # Health Check
        location /health {
            proxy_pass http://api_servers;
            proxy_http_version 1.1;
            access_log off;
        }
    }
}
```

### 3. Database Migration

```bash
# Masuk ke container API
docker compose exec api sh

# Run Prisma migrations
npx prisma migrate deploy

# Generate Prisma client
npx prisma generate

# Seed initial data (optional)
npx prisma db seed
```

### 4. Setup Automatic Backups

```bash
# Buat script backup
cat > /opt/simrs-zen/backup.sh << 'EOF'
#!/bin/bash
BACKUP_DIR="/opt/simrs-zen/backups"
DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_FILE="$BACKUP_DIR/simrs_zen_$DATE.sql.gz"

mkdir -p $BACKUP_DIR

# Backup PostgreSQL
docker compose exec -T postgres pg_dump -U simrs_admin simrs_zen_production | gzip > $BACKUP_FILE

# Keep only last 7 days
find $BACKUP_DIR -name "*.sql.gz" -mtime +7 -delete

echo "Backup completed: $BACKUP_FILE"
EOF

chmod +x /opt/simrs-zen/backup.sh

# Setup cron job (daily at 2 AM)
(crontab -l 2>/dev/null; echo "0 2 * * * /opt/simrs-zen/backup.sh >> /var/log/simrs-backup.log 2>&1") | crontab -
```

---

## 📊 Monitoring & Maintenance

### View Logs

```bash
# All services
docker compose logs -f

# Specific service
docker compose logs -f api
docker compose logs -f postgres
docker compose logs -f nginx

# Last 100 lines
docker compose logs --tail=100 api
```

### Container Management

```bash
# Restart specific service
docker compose restart api

# Stop all services
docker compose down

# Stop and remove volumes (CAUTION!)
docker compose down -v

# Rebuild and restart
docker compose up -d --build --force-recreate
```

### Resource Monitoring

```bash
# Container stats
docker stats

# Disk usage
docker system df

# Clean unused resources
docker system prune -a
```

### Database Maintenance

```bash
# Connect to PostgreSQL
docker compose exec postgres psql -U simrs_admin simrs_zen_production

# Vacuum and analyze
docker compose exec postgres psql -U simrs_admin -c "VACUUM ANALYZE;" simrs_zen_production

# Check table sizes
docker compose exec postgres psql -U simrs_admin -c "
SELECT relname AS table_name,
       pg_size_pretty(pg_total_relation_size(relid)) AS size
FROM pg_catalog.pg_statio_user_tables
ORDER BY pg_total_relation_size(relid) DESC
LIMIT 10;
" simrs_zen_production
```

### Redis Monitoring

```bash
# Connect to Redis CLI
docker compose exec redis redis-cli

# Check memory usage
docker compose exec redis redis-cli INFO memory

# Clear all cache
docker compose exec redis redis-cli FLUSHALL
```

---

## 🔍 Troubleshooting

### Container Won't Start

```bash
# Check logs
docker compose logs api

# Check container status
docker compose ps -a

# Inspect container
docker inspect simrs-zen-api
```

### Database Connection Issues

```bash
# Test database connection
docker compose exec api sh -c "nc -zv postgres 5432"

# Check PostgreSQL logs
docker compose logs postgres

# Verify environment variables
docker compose exec api env | grep DATABASE
```

### High Memory Usage

```bash
# Check memory per container
docker stats --no-stream

# Limit container memory in docker-compose.yml
# Add under service:
#   deploy:
#     resources:
#       limits:
#         memory: 2G
```

### SSL Certificate Renewal

```bash
# Manual renewal
sudo certbot renew

# Copy new certificates
sudo cp /etc/letsencrypt/live/api.simrszen.id/*.pem /opt/simrs-zen/ssl/

# Restart nginx
docker compose restart nginx
```

### Reset Everything

```bash
# Stop all
docker compose down

# Remove volumes (WARNING: loses all data!)
docker compose down -v

# Remove images
docker compose down --rmi all

# Fresh start
docker compose up -d --build
```

---

## 📱 Health Checks

### API Health Check

```bash
curl -s https://api.simrszen.id/health | jq
```

Expected response:
```json
{
  "status": "ok",
  "timestamp": "2024-01-15T10:30:00.000Z",
  "version": "1.0.0",
  "service": "SIMRS ZEN API"
}
```

### Database Health Check

```bash
docker compose exec postgres pg_isready -U simrs_admin
```

### Redis Health Check

```bash
docker compose exec redis redis-cli ping
```

---

## 🔐 Security Checklist

- [ ] Change default passwords in `.env`
- [ ] Enable firewall (ufw)
- [ ] Setup SSL certificates
- [ ] Configure rate limiting
- [ ] Enable automatic backups
- [ ] Setup log rotation
- [ ] Configure fail2ban
- [ ] Regular security updates

```bash
# Firewall setup
sudo ufw default deny incoming
sudo ufw default allow outgoing
sudo ufw allow ssh
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw enable
```

---

## 📞 Support

Jika mengalami kendala:
1. Cek dokumentasi di `/docs`
2. Review logs dengan `docker compose logs`
3. Hubungi tim support: support@simrszen.id

---

**Last Updated:** February 2026  
**Version:** 1.0.0
