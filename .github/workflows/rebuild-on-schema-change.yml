name: Rebuild Backend on Schema Change

on:
  push:
    branches:
      - main
    paths:
      - 'prisma/schema.prisma'
      - 'backend/prisma/schema.prisma'

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ghcr.io/${{ github.repository }}/simrs-zen-api

jobs:
  rebuild-backend:
    name: Rebuild & Push Backend Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout kode
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push backend Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          tags: ${{ env.IMAGE_NAME }}:latest
          labels: org.opencontainers.image.source=https://github.com/${{ github.repository }}
          cache-from: type=gha,scope=backend-schema
          cache-to: type=gha,scope=backend-schema,mode=max

      - name: Log rebuild result
        run: |
          echo "✅ Backend image rebuilt and pushed to ${{ env.IMAGE_NAME }}:latest"
          echo "Triggered by schema change in commit ${{ github.sha }}"
